# RP03 — Booking & Services

This repository is dedicated to **RP03 — Booking & Services**.

RP03 product implementation has not started. No application stack, architecture, framework, database, API, UI, deployment model, or product implementation is approved or authorized.

This initial README is the one-time owner-authorized seed exception required to establish the `main` branch in an otherwise empty repository.

After this seed exception:

- direct writes to `main` are prohibited;
- every subsequent change must be made on a dedicated branch; and
- every subsequent change must be reviewed through a pull request before any authorized merge.
